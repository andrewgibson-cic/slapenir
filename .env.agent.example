# ============================================================================
# SLAPENIR Agent Environment - DUMMY CREDENTIALS (Example)
# ============================================================================
# Copy this file to .env.agent (should work as-is with dummy values)
# 
# cp .env.agent.example .env.agent
# 
# No need to edit - these dummy values are correct and will be replaced by proxy
# ============================================================================

# LLM API Keys (DUMMY - These get replaced by proxy)
OPENAI_API_KEY=DUMMY_OPENAI
ANTHROPIC_API_KEY=DUMMY_ANTHROPIC
GEMINI_API_KEY=DUMMY_GEMINI
MISTRAL_API_KEY=DUMMY_MISTRAL

# Cloud Provider Keys (DUMMY - These get replaced by proxy)
AWS_ACCESS_KEY_ID=DUMMY_AWS_ACCESS
AWS_SECRET_ACCESS_KEY=DUMMY_AWS_SECRET
AWS_REGION=us-east-1

# Version Control (DUMMY - These get replaced by proxy)
GITHUB_TOKEN=DUMMY_GITHUB
GITLAB_TOKEN=DUMMY_GITLAB

# Proxy Configuration (Required - DO NOT CHANGE)
HTTP_PROXY=http://proxy:3000
HTTPS_PROXY=http://proxy:3000
NO_PROXY=localhost,127.0.0.1,proxy

# Python Configuration
PYTHONUNBUFFERED=1

# mTLS Configuration
MTLS_ENABLED=false
MTLS_CA_CERT=/certs/root_ca.crt
MTLS_CLIENT_CERT=/certs/agent.crt
MTLS_CLIENT_KEY=/certs/agent.key
MTLS_VERIFY_HOSTNAME=true