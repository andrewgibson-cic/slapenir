#!/bin/bash
set -e

REPO_PATH="${1:-.}"

if [ ! -d "$REPO_PATH" ]; then
    echo "Error: Directory not found: $REPO_PATH"
    exit 1
fi

REPO_PATH=$(cd "$REPO_PATH" && pwd)

echo "Indexing repository at $REPO_PATH..."
cgr start --repo-path "$REPO_PATH" --update-graph --clean
